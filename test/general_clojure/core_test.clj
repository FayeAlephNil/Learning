(ns general-clojure.core-test
  (:require [clojure.test :refer :all]
            [general-clojure.core :refer :all]))

(deftest a-test
  (testing "Always returns true because we aren't doing anything in core yet :P"
    true))
